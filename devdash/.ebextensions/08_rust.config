# .ebextensions/09_rust.config
commands:
  01_install_rust:
    command: |
      curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
      source $HOME/.cargo/env
      echo "export PATH=$HOME/.cargo/bin:$PATH" >> /etc/profile.d/rust.sh
      chmod +x /etc/profile.d/rust.sh